{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Generate Python Env Requirements",
            "type": "shell",
            "command": "${command:python.interpreterPath} -m pipreqs .",
            "problemMatcher": [],
            "options": {
                "env": {
                    "PYTHONPATH": "${workspaceFolder}"
                }
            }
        }
    ]
}